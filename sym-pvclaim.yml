apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sym-pv-claim
  namespace: backend
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
